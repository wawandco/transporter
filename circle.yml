# Both postgresql and mysql are set up to use the ubuntu user, have a database called circle_test available
machine:
  services:
    - postgresql
dependencies:
  override:
    - echo "skipping dependencies"
    - mkdir -p /home/ubuntu/.go_workspace/src/github.com/wawandco/transporter
    - cp -R . /home/ubuntu/.go_workspace/src/github.com/wawandco/transporter
