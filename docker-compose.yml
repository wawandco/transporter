db:
  image: postgres
  environment:
    LC_ALL: C.UTF-8

transporter:
  build: .
  links:
    - db:postgres.local
  environment:
    TEST_DATABASE_URL:   "user=postgres dbname=ttest sslmode=disable host=postgres.local" #postgres://:@:5432/ttest
