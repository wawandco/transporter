transporter:
  build: .
  links:
    - postgres
  environment:
    - TRANS_TESTING_FOLDER=/go/src/testing
    - TEST_DATABASE_URL=user=postgres dbname=ttest sslmode=disable host=postgres"

postgres:
  image: postgres:9.4
